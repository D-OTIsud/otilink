# Supabase (required)
# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-side only: for public profile page data (bypasses RLS)
# Get from Supabase: Settings > API > service_role (keep secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: canonical site URL (e.g. for OAuth redirects and dashboard links)
# NEXT_PUBLIC_SITE_URL=https://links.otisud.re

# Webhook secret to purge server-side cache tags
REVALIDATE_SECRET=choose-a-long-random-secret
